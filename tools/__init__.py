# ---------------------------------------------------------------------------
# tools/__init__.py
# Package marker for the AI Sales Enablement tool suite.
# Each module in this package exposes a single high-level function that can
# be called from the CLI runner (run_tools.py) or the Streamlit app (app.py).
# All tools default to "mock mode" so they produce realistic demo output
# without requiring any API keys.
# ---------------------------------------------------------------------------
